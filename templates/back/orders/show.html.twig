{% extends 'back/base.html.twig' %}

{% block title %}Order Details{% endblock %}

{% block content %}
<div class="page-inner">
    <div class="page-header">
        <h4 class="page-title">Order Details</h4>
        <ul class="breadcrumbs">
            <li class="nav-home">
                <a href="{{ path('admin_dashboard') }}">
                    <i class="flaticon-home"></i>
                </a>
            </li>
            <li class="separator">
                <i class="flaticon-right-arrow"></i>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_orders_index') }}">Orders</a>
            </li>
            <li class="separator">
                <i class="flaticon-right-arrow"></i>
            </li>
            <li class="nav-item">
                <span>Order #{{ order.id }}</span>
            </li>
        </ul>
    </div>
    
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                {{ message }}
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
        {% endfor %}
    {% endfor %}
    
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <div class="d-flex align-items-center">
                        <h4 class="card-title">Order #{{ order.id }}</h4>
                        <div class="ml-auto">
                            <a href="{{ path('admin_orders_index') }}" class="btn btn-primary btn-sm shadow-sm rounded-pill"><i class="fas fa-arrow-left mr-2"></i> Back to Orders</a>
                        </div>
                    </div>
                </div>
                
                <div class="card-header bg-light">
                    <div class="row align-items-center">
                        <div class="col-md-12 mb-2">
                            <h5 class="mb-0">Change Order Status</h5>
                        </div>
                        <div class="col-md-12 d-flex flex-wrap justify-content-center">
                            <form method="GET" action="{{ path('admin_orders_update_status', {'id': order.id, 'status': 'pending'}) }}" class="mx-2 mb-2" onsubmit="return confirmStatusChange('pending');">
                                <button type="submit" class="btn btn-warning btn-md shadow-sm rounded-pill px-4"><i class="fas fa-clock mr-2"></i> Mark as Pending</button>
                            </form>
                            <form method="GET" action="{{ path('admin_orders_update_status', {'id': order.id, 'status': 'done'}) }}" class="mx-2 mb-2" onsubmit="return confirmStatusChange('done');">
                                <button type="submit" class="btn btn-success btn-md shadow-sm rounded-pill px-4"><i class="fas fa-check-circle mr-2"></i> Mark as Done</button>
                            </form>
                            <form method="GET" action="{{ path('admin_orders_update_status', {'id': order.id, 'status': 'canceled'}) }}" class="mx-2 mb-2" onsubmit="return confirmStatusChange('canceled');">
                                <button type="submit" class="btn btn-danger btn-md shadow-sm rounded-pill px-4"><i class="fas fa-times-circle mr-2"></i> Mark as Canceled</button>
                            </form>
                            <form method="POST" action="{{ path('admin_orders_delete', {'id': order.id}) }}" class="mx-2 mb-2" onsubmit="return confirm('Are you sure you want to delete this order?');">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order.id) }}">
                                <button type="submit" class="btn btn-danger btn-md shadow-sm rounded-pill px-4"><i class="fas fa-trash-alt mr-2"></i> Delete Order</button>
                            </form>
                        </div>
                    </div>
                </div>
                
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="card card-stats card-round">
                                <div class="card-body">
                                    <div class="row align-items-center">
                                        <div class="col-icon">
                                            <div class="icon-big text-center icon-primary bubble-shadow-small">
                                                <i class="fas fa-user"></i>
                                            </div>
                                        </div>
                                        <div class="col col-stats ml-3 ml-sm-0">
                                            <div class="numbers">
                                                <p class="card-category">Customer Information</p>
                                                <h4 class="card-title">{{ order.customerName }}</h4>
                                                <p class="card-text">{{ order.customerEmail }}</p>
                                                <p class="card-text">Phone: {{ order.phone }}</p>
                                                <p class="card-text">Size: {{ order.size }}</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-6">
                            <div class="card card-stats card-round">
                                <div class="card-body">
                                    <div class="row align-items-center">
                                        <div class="col-icon">
                                            <div class="icon-big text-center icon-info bubble-shadow-small">
                                                <i class="fas fa-shipping-fast"></i>
                                            </div>
                                        </div>
                                        <div class="col col-stats ml-3 ml-sm-0">
                                            <div class="numbers">
                                                <p class="card-category">Status</p>
                                                <form method="GET" id="statusForm">
                                                    <select name="status" class="form-control form-control-sm" onchange="updateOrderStatus(this.value);">
                                                        <option value="pending" {{ order.status == 'pending' ? 'selected' : '' }}>Pending</option>
                                                        <option value="done" {{ order.status == 'done' ? 'selected' : '' }}>Done</option>
                                                        <option value="canceled" {{ order.status == 'canceled' ? 'selected' : '' }}>Canceled</option>
                                                    </select>
                                                </form>
                                                <p class="card-text mt-2">Order Date: {{ order.createdAt|date('Y-m-d H:i') }}</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6">
                            <div class="card card-stats card-round">
                                <div class="card-body">
                                    <div class="row align-items-center">
                                        <div class="col-icon">
                                            <div class="icon-big text-center icon-success bubble-shadow-small">
                                                <i class="fas fa-map-marker-alt"></i>
                                            </div>
                                        </div>
                                        <div class="col col-stats ml-3 ml-sm-0">
                                            <div class="numbers">
                                                <p class="card-category">Shipping Address</p>
                                                <p class="card-text">{{ order.shippingAddress }}</p>
                                                <p class="card-text">City: {{ order.city }}</p>
                                                <p class="card-text">Postal Code: {{ order.postalCode }}</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-6">
                            <div class="card card-stats card-round">
                                <div class="card-body">
                                    <div class="row align-items-center">
                                        <div class="col-icon">
                                            <div class="icon-big text-center icon-secondary bubble-shadow-small">
                                                <i class="fas fa-credit-card"></i>
                                            </div>
                                        </div>
                                        <div class="col col-stats ml-3 ml-sm-0">
                                            <div class="numbers">
                                                <p class="card-category">Billing Address</p>
                                                <p class="card-text">{{ order.billingAddress ? order.billingAddress : order.shippingAddress }}</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="separator-solid"></div>
                    
                    <div class="row">
                        <div class="col-md-12">
                            <h4>Order Items</h4>
                            <div class="table-responsive">
                                <table class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>Product</th>
                                            <th>Size</th>
                                            <th>Quantity</th>
                                            <th>Price</th>
                                            <th class="text-right">Total</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {% for item in order.items %}
                                            <tr>
                                                <td>
                                                    <div class="d-flex align-items-center">
                                                        {% if item.product.imagePath %}
                                                            <img src="{{ asset('uploads/products/' ~ item.product.imagePath) }}" alt="{{ item.product.name }}" class="mr-3" style="width: 50px; height: 50px; object-fit: cover;">
                                                        {% else %}
                                                            <div class="mr-3" style="width: 50px; height: 50px; background-color: #eee; display: flex; align-items: center; justify-content: center;">
                                                                <i class="fas fa-image text-muted"></i>
                                                            </div>
                                                        {% endif %}
                                                        <span>{{ item.product.name }}</span>
                                                    </div>
                                                </td>
                                                <td>{{ item.size }}</td>
                                                <td>{{ item.quantity }}</td>
                                                <td>${{ item.price }}</td>
                                                <td class="text-right">${{ (item.price * item.quantity)|number_format(2) }}</td>
                                            </tr>
                                        {% else %}
                                            <tr>
                                                <td colspan="5" class="text-center">No items found for this order</td>
                                            </tr>
                                        {% endfor %}
                                    </tbody>
                                    <tfoot>
                                        <tr>
                                            <td colspan="4" class="text-right"><strong>Total:</strong></td>
                                            <td class="text-right"><strong>${{ order.totalAmount }}</strong></td>
                                        </tr>
                                    </tfoot>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function() {
            $('#recent-orders').DataTable({
                "pageLength": 5,
                "lengthMenu": [[5, 10, 25, 50, -1], [5, 10, 25, 50, "All"]]
            });
        });
        
        function updateOrderStatus(status) {
            if (confirm('Are you sure you want to change the order status to ' + status + '?')) {
                window.location.href = '{{ path('admin_orders_update_status', {'id': order.id, 'status': 'STATUS_PLACEHOLDER'}) }}'.replace('STATUS_PLACEHOLDER', status);
            }
        }
    </script>
{% endblock %}
